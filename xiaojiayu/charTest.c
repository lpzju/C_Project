#include <stdio.h>

int main(){
    char ch;
    scanf("%c",&ch);
    if (ch == 'a') {
        printf("hello");
    }
    if (ch == 'b') {
        printf("world");
    }
    printf("\n");
    return 0;
}
